# Git attributes — UE5 仓库（最省方案，不使用 LFS）
#
# 行尾归一化：文本文件入库统一为 LF，检出按平台自动处理。
* text=auto

# UE5 资产是二进制，禁止 git 当文本 diff/merge（即使是小蓝图也按二进制处理）。
*.uasset binary
*.umap binary

# 图片按二进制。
*.png binary
*.jpg binary
*.jpeg binary
*.tga binary
*.dds binary
*.exr binary

# 配置文件用 LF。
*.ini text eol=lf

# 脚本/文档。
*.md text eol=lf
*.sh text eol=lf
